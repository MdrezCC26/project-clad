engine-strict=true
shamefully-hoist=true
; Local dev + Docker first stage need devDependencies (TypeScript, Vite, @shopify/polaris-types).
; Some machines set global `omit=dev`, which breaks `npm ci` here.
include=dev
