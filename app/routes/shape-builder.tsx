/**
 * App-proxy alias: Shopify forwards `/apps/project-clad/shape-builder` as `/shape-builder`.
 * Keep in sync with `apps.project-clad.shape-builder.tsx`.
 */
export { loader, default } from "./apps.project-clad.shape-builder";
