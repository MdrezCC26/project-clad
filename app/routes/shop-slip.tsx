/**
 * App-proxy alias: Shopify forwards `/apps/project-clad/shop-slip` as `/shop-slip`.
 * Keep in sync with `apps.project-clad.shop-slip.tsx`.
 */
export { loader } from "./apps.project-clad.shop-slip";
